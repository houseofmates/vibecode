# Hermes Web UI dependencies
# Core runtime (stdlib only - no pinned versions needed)
# pyyaml: optional, for config.yaml loading. auto-installed if missing.

# For full functionality with hermes-agent integration:
# Run the server once and it will auto-install agent dependencies.
# Or install hermes-agent separately and point via HERMES_WEBUI_AGENT_DIR

# Development / testing (optional)
# pytest>=7.0.0
# playwright>=1.40.0